<!-- Page Content -->
<div class="container">   
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Profile</h1>
        </div>
    </div>

    <p><strong><%= session["user"].email %></strong></p>
    <ul>
    <% @songs.each do |song| %>
        <li><%= song.title %> by <%= song.author %></li>
    <% end %>
    </ul>
    
</div>